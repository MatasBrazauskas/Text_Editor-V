Before starting:

Normal Mode:
operations: d, y,
motions: hjkl, w, W, e, E, b,B G $, 0, ^, 
text ojbect: f, F, r

Unit tests:
Parsing tests, wWeEbB tests.

Insert mode:
backspace support, enter support

Tests: backspace and enter unit tests;

Draw backs:
not enought unit tests,
monolith, tight coupling,
graphics AI

